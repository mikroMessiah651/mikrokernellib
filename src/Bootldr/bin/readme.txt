to run:
	either assemble the .asm files and then use binary concatenation: cat 1.bin 2.bin > combined.bin

	or run the binaries in bin/


note: infinite loop to be debugged somewhere in bootld-stage2-x86.asm
the binary still technically counts as a bootloader
